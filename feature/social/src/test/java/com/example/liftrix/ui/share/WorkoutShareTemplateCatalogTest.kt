package com.example.liftrix.ui.share

/*
 * Targeted catalog cases for the module once feature:social unit-test
 * dependencies are enabled:
 * - catalog exposes at least four bundled asset templates
 * - default template is deterministic
 * - unknown ids resolve to the default template
 * - all template asset paths stay under workout_share_backgrounds/
 */
