package com.example.liftrix.ui.share

/*
 * Targeted formatter cases for the module once feature:social unit-test
 * dependencies are enabled:
 * - long workout names clamp for story preview/export
 * - missing duration produces null duration and no blank preview gap
 * - empty PR list formats as "No PRs this session"
 * - PR labels are capped to three visible rows
 * - total volume uses Workout.calculateTotalVolumeKg()
 */
