package com.example.liftrix.ui.social.comments

/*
 * Targeted ViewModel cases for the module once feature:social unit-test
 * dependencies are enabled:
 * - authenticated viewer ID is passed to getPostComments
 * - a comment report uses the comment ID, author ID, and COMMENT content type
 * - repository report failure becomes PostCommentsUiState.Error
 * - a stale report dialog target is rejected before repository submission
 */
