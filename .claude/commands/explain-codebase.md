Scan the entire codebase and provide a clear, high-level explanation of how the application functions, focusing specifically on the UI/UX flow.

Describe how the user interface is structured and how components are rendered.

Trace what happens in the code when a user clicks a button — from the UI event to the underlying logic or API call.

Show how different parts of the frontend and backend are connected, especially around key user interactions.

Explain in simple terms, avoiding overly technical jargon where possible, so I can understand the logic and structure clearly.